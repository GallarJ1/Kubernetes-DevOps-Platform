apiVersion: v1
kind: Service
metadata:
  name: devops-platform-api-service
spec:
  selector:
    app: devops-platform-api
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: NodePort